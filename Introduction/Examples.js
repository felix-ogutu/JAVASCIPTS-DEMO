//Programme to find the square root of the number
//Take the input from the user
//let number = prompt('Enter the number :');
//let result = Math.sqrt(number);
//console.log(`The square root of ${number} is the ${result}`);

//Programme to find the area of the Triangle
//Take the input from the user
let height=prompt("Enter the height of the Triangle :")
let length=prompt("Enter the length of the Triangle :")
let base=prompt("Enter the base of the Triangle :")

//Calculate the result
let result=(height*length*base);
console.log(`The result of area of the Triangle ${result}`);